# YWD Packet

Native Android AX.25 packet terminal for TCP KISS TNCs, built around the YWD-MMDVM-TNC project.

> Early development. Active work is staged on the `dev` branch before promotion to `main`.
